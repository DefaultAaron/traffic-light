"""KD pre-implementation design spikes (GPU-free, rehearsal-only).

Spikes validate cell-level feasibility before runner implementation. Outputs are
rehearsal_-prefixed JSON files under runs/, never ship-decision artifacts.
"""
