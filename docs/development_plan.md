# 交通信号灯识别 — 开发计划v1.0

## 项目概述

为自动驾驶系统开发交通信号灯检测与状态分类模块，部署于 **NVIDIA Tegra Orin (64GB)** 平台。该平台同时运行完整的自动驾驶软件栈（感知、规划、控制、传感器融合等），因此信号灯模块必须轻量化，尽量减少 GPU/内存占用。

---

## 一、候选模型

| 模型 | 参数量 | COCO mAP | 免NMS | 定位 |
|------|--------|----------|-------|------|
| **YOLO26-n** | ~2.5M | 40.9% | 是 | 主力候选（最轻量） |
| **YOLO26-s** | ~9M | 47.5% | 是 | 主力候选（精度更优） |
| YOLO26-m | ~20M | 51.5% | 是 | 备选（需验证资源占用） |
| **YOLO11-n** | ~2.6M | 39.5% | 否 | 主力候选（稳定基线） |
| **YOLO11-s** | ~9M | 43.5% | 否 | 主力候选 |
| YOLO11-m | ~20M | 50.4% | 否 | 备选 |
| **RT-DETR-L** | ~32M | ~53.0% | 是 | 仅作精度上限参考，不作部署 |

### 选型说明

- **YOLO26**：最新版本（2026年1月），免NMS端到端推理，延迟更稳定；STAL（小目标感知标签分配）有助于远距离信号灯检测
- **YOLO11**：成熟稳定，社区资源丰富，作为对比基线
- **RT-DETR-L**：Transformer架构，精度最高，但参数量过大（~32M），不适合部署，仅作精度天花板参考
- 所有模型均通过 Ultralytics 统一训练框架，确保公平对比

---

## 二、数据集

| 数据集 | 图片数 | 标注数 | 分辨率 | 标注格式 | 许可证 |
|--------|--------|--------|--------|----------|--------|
| **S2TLD** | 1,222 | 2,436 | 1920×1080 | Pascal VOC XML | MIT |
| **BSTLD** | 13,427 | ~24,000 | 1280×720 | YAML | 非商用 |
| **LISA** | 43,075 | ~226,000 | 多种 | CSV | CC BY-NC-SA |
| **合计** | ~57,724 | ~252,000 | — | — | — |

### 原始类别

- **S2TLD（5类）**：`red`, `green`, `yellow`, `wait_on`, `off`
- **BSTLD 训练集（13类）**：Red, RedLeft, RedRight, RedStraight, RedStraightLeft, Green, GreenLeft, GreenRight, GreenStraight, GreenStraightLeft, GreenStraightRight, Yellow, Off
- **BSTLD 测试集（4类）**：Red, Green, Yellow, Off（无方向标注，第二阶段需重标注）
- **LISA（7类）**：`go`(99K), `stop`(90K), `stopLeft`(26K), `warning`(5.5K), `goLeft`(5K), `warningLeft`(701), `goForward`(410)

---

## 三、开发阶段

### 第一阶段：基础颜色检测（当前）

**目标**：检测信号灯并分类为红/黄/绿三类

| 类别ID | 类别 | 说明 |
|--------|------|------|
| 0 | `red` | 红灯（含所有方向变体） |
| 1 | `yellow` | 黄灯（含所有方向变体） |
| 2 | `green` | 绿灯（含所有方向变体） |

**类别映射规则**：
- 所有方向变体合并为基础颜色（如 `stopLeft` → red，`goForward` → green）
- 过滤 `off`、`wait_on` 等非可操作状态
- 三个数据集全部参与训练

**工作内容**：
1. 编写数据转换脚本（S2TLD/BSTLD/LISA → 统一 YOLO 格式）
2. 合并数据集，80/20 分层划分训练/验证集
3. 训练 5-7 个模型变体（YOLO26 n/s/m, YOLO11 n/s/m, RT-DETR-L）
4. 对比评估：mAP、逐类精度、推理延迟、GPU 占用、模型大小
5. 选定最优模型架构与尺寸

**训练策略**：

- 使用 COCO 预训练权重进行迁移学习（非从零训练）
- ~100 epochs，基于验证集 mAP 早停
- 禁用垂直翻转（`flipud=0.0`），保留 HSV 增强
- 合并数据集统一训练（非逐数据集顺序训练，避免灾难性遗忘）

### 第二阶段：方向检测（后续）

**目标**：在颜色基础上增加灯型识别（圆灯 / 左转箭头 / 直行箭头 / 右转箭头），共 9 类

| 类别ID | 类别 | 说明 |
|--------|------|------|
| 0 | `red` | 红色圆灯 |
| 1 | `yellow` | 黄色圆灯（含所有黄灯方向变体） |
| 2 | `green` | 绿色圆灯 |
| 3 | `redLeft` | 红色左转箭头 |
| 4 | `greenLeft` | 绿色左转箭头 |
| 5 | `redForward` | 红色直行箭头 |
| 6 | `greenForward` | 绿色直行箭头 |
| 7 | `redRight` | 红色右转箭头 |
| 8 | `greenRight` | 绿色右转箭头 |

**设计原则**：
- 检测模型只负责识别**看到了什么**（颜色 + 灯型），规划模块决定**怎么做**
- 圆灯含义的歧义由规划模块解决
  - 示例：检测到 `greenForward` + `redLeft` + `redRight` → 可直行，不可转弯
  - 示例：检测到 `green`（圆灯）→ 所有方向均可通行
  - 示例：检测到 `green`（圆灯）+ `redLeft` → 可直行/右转，不可左转
- 组合多方向标签（如 BSTLD 的 `GreenStraightLeft`、`RedStraightLeft`、`GreenStraightRight`）→ 跳过（合计仅 5 条，视觉上为箭头形态而非圆灯，且在中国实际路况中极为罕见）
- 黄灯方向变体 → 黄色圆灯（仅 LISA 有 350 条 `warningLeft`，其余为零）
- **S2TLD 标注说明**：原始 S2TLD 无方向标注且存在漏标。`Annotations-fix/` 为人工重标注版本，已补充方向标签（redLeft, greenLeft）。normal_1 和 normal_2 子集仍为原始3类标注

**各数据集方向标注覆盖情况**：

| 数据集 | 圆灯 (R/Y/G) | 左转箭头 (R/G) | 直行箭头 (R/G) | 右转箭头 (R/G) | 说明 |
|--------|--------------|----------------|----------------|----------------|------|
| S2TLD | 全部 → 圆灯 | redLeft (572), greenLeft (77) | 无 | 无 | 方向标注仅限 Annotations-fix（重标注版）；normal_1/normal_2 为3类 |
| BSTLD | 有 | RedLeft (1,092), GreenLeft (178) | RedStraight (9), GreenStraight (20) | RedRight (5), GreenRight (13) | 组合多方向标签跳过（仅5条） |
| LISA | 有 | stopLeft (12,734), goLeft (2,476) | goForward (205) | 无 | warningLeft → 黄色圆灯 |

---

## 四、环境与基础设施

| 环境 | 硬件 | 用途 |
|------|------|------|
| 开发 | MacBook M4 Pro 24GB | 本地开发、快速验证、CoreML 导出测试 |
| 训练 | GPU 服务器（远程） | 完整训练、超参搜索 |
| 部署 | NVIDIA Tegra Orin 64GB | 最终部署目标 — 与完整自动驾驶栈共享资源 |

**导出格式**：TensorRT FP16（Orin 部署）、CoreML（M4 Pro 开发）、ONNX（通用）

---

## 五、已知问题与风险

### 1. 数据标注问题

| 问题 | 详情 | 影响阶段 |
|------|------|----------|
| **方向标注不一致** | S2TLD 方向标注仅限原始子集；LISA 有左转和少量直行；BSTLD 训练集有方向但稀缺，测试集（8,334张）无方向标注需重标注 | 第二阶段 |
| **直行箭头数据稀缺** | BSTLD 29 条（RedStraight 9 + GreenStraight 20）、LISA 205 条（goForward），需补充数据 | 第二阶段 |
| **右转箭头数据极度稀缺** | 仅 BSTLD 有右转标注（RedRight 5 条、GreenRight 13 条），需重标注或采集新数据 | 第二阶段 |
| **黄灯样本偏少** | S2TLD 黄灯仅 75 个标注（红灯 1235，绿灯 816）；LISA `warning` 仅 5.5K（`go` 99K） | 两个阶段 |
| **第二阶段需要重新标注** | 直行箭头（合计 234 条）和右转箭头（合计 18 条）数据严重不足，需人工补标或采集新数据 | 第二阶段 |

### 2. 商用许可问题

| 组件 | 许可证 | 研究可用 | 商用可用 |
|------|--------|----------|----------|
| Ultralytics YOLO（所有版本） | AGPL-3.0 | 是 | 需购买企业许可 |
| RT-DETR（Ultralytics 版） | AGPL-3.0 | 是 | 需购买企业许可 |
| RT-DETR（百度原版） | Apache-2.0 | 是 | 是 |
| S2TLD 数据集 | MIT | 是 | 是 |
| BSTLD 数据集 | 非商用 | 是 | **否** |
| LISA 数据集 | CC BY-NC-SA | 是 | **否** |

**商用化路径**：

- 研究阶段可使用全部数据集和框架
- 生产部署需要：
  - 购买 Ultralytics 企业许可证，或基于 Apache-2.0 的框架重新实现
  - 仅使用 S2TLD（MIT）数据，或采集/购买商用授权数据集
  - 采集实际驾驶场景数据并标注（同时解决域差异问题）

### 3. 所需资源

| 资源 | 用途 | 优先级 |
|------|------|--------|
| **GPU 训练服务器** | 完整训练 5-7 个模型变体（M4 Pro 仅适合快速验证） | 高 — 第一阶段 |
| **Tegra Orin 开发板** | 部署测试、TensorRT 推理延迟实测、GPU 占用评估 | 高 — 第一阶段 |
| **Ultralytics 企业许可** | 商用部署（如选用 YOLO/RT-DETR Ultralytics 版） | 生产阶段 |
| **标注工具 + 标注人力** | 第二阶段方向标注补充、生产阶段自有数据标注 | 第二阶段 |
| **实际驾驶数据采集** | 商用数据集构建、覆盖中国路况信号灯样式 | 生产阶段 |

### 4. 技术风险

| 风险 | 说明 | 缓解措施 |
|------|------|----------|
| 黄灯检测精度不足 | 黄灯样本远少于红/绿灯，可能影响召回率 | 数据增强（HSV 扰动）、过采样、关注 per-class 指标 |
| 远距离小目标漏检 | 信号灯在远处可能仅占几个像素 | YOLO26 STAL 机制、copy-paste 增强、评估 AP-small |
| Orin 资源超标 | 模型 GPU 占用超出预算（<5% 目标） | 优先选用 nano 变体、TensorRT FP16 优化 |
| 数据集域差异 | 三个数据集来自不同国家/场景，可能与中国路况差异较大 | 评估模型在实际场景的表现，必要时采集本地数据微调 |

---

## 六、里程碑

| 阶段 | 里程碑 | 交付物 |
|------|--------|--------|
| 第一阶段-数据 | 数据转换与合并完成 | 统一 YOLO 格式数据集、转换脚本、数据集配置文件 |
| 第一阶段-训练 | 全部模型训练完成 | 5-7 个训练好的模型权重（YOLO26 n/s/m, YOLO11 n/s/m, RT-DETR-L） |
| 第一阶段-评估 | 模型对比评估完成 | 评估报告：mAP@0.5、mAP@0.5:0.95、逐类精度（红/黄/绿）、AP-small |
| 第一阶段-导出 | 多平台模型导出完成 | TensorRT FP16（Orin）、CoreML（M4 Pro 开发）、ONNX（通用） |
| 第一阶段-部署 | Orin 端测试通过 | 推理延迟实测数据、GPU 显存占用报告、与自动驾驶栈并行运行验证 |
| 第一阶段-选型 | 最优模型架构与尺寸确定 | 选型报告（综合精度、延迟、资源占用的最终推荐） |
| 第二阶段-规划 | 方向检测方案确定 | 9 类标注规范、数据缺口分析、数据补充计划 |
| 第二阶段-重标注 | 方向标注重标注 | S2TLD normal_1 (796张) / normal_2 (4,168张) 补充方向标签；BSTLD 测试集 (8,334张) 补充方向标签 |
| 第二阶段-数据采集 | 直行/右转箭头数据补充 | 从现有数据集补标 + 采集实际驾驶场景数据（兼顾中国路况信号灯样式差异） |
| 第二阶段-数据合并 | 方向标注数据准备完成 | 9 类统一格式数据集、按9类分层划分 |
| 第二阶段-训练 | 9 类模型训练完成 | 方向检测模型权重 |
| 第二阶段-评估 | 方向检测模型评估完成 | 9 类逐类精度报告、与第一阶段基线对比 |
| 第二阶段-部署 | 9 类模型 Orin 端测试通过 | TensorRT 导出模型、延迟/占用实测数据 |
| 生产化-数据 | 商用数据集构建完成 | MIT/自有授权数据集、实际驾驶场景数据（覆盖中国路况信号灯样式） |
| 生产化-许可 | 商用许可问题解决 | Ultralytics 企业许可证 或 Apache-2.0 框架重新实现方案 |
| 生产化-部署 | 生产环境上线 | 最终生产模型、部署文档、性能基准报告 |
